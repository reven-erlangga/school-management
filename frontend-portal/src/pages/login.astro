---
import LoginForm from "../modules/form/LoginForm.svelte";
import LoginVisual from "../components/svelte/organisms/landing/LoginVisual.svelte";
import AccessNavbar from "../components/svelte/organisms/AccessNavbar.svelte";
import AccessLayout from "@layouts/AccessLayout.astro";
---

<AccessLayout title="Masuk | Admin Portal">
  <!-- Global Navigation -->
  <AccessNavbar client:load />

  <main class="min-h-screen grid lg:grid-cols-2 bg-slate-50 dark:bg-slate-950 text-slate-900 dark:text-white transition-colors duration-500">
    <!-- Left: Visual -->
    <section class="hidden lg:block relative min-h-screen">
      <LoginVisual client:load />
    </section>

    <!-- Right: Form -->
    <section class="flex items-center justify-center px-6 py-16 relative z-10">
      <div class="w-full max-w-md">
        <LoginForm client:load />
      </div>
    </section>
  </main>
</AccessLayout>
