apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: school-management-prod

resources:
- ../../../base/vault
- pvc.yaml

patches:
- path: vault-patch.yaml

configMapGenerator:
- name: vault-config
  files:
  - vault.hcl
